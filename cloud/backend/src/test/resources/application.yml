homesense:
  mqtt:
    enabled: false
    client-id: homesense-test

spring:
  datasource:
    url: jdbc:h2:mem:homesense;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
