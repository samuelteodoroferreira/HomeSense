# HOME_SENSE — Mosquitto em Docker (desenvolvimento / demo)
# ATENÇÃO: allow_anonymous true é só para lab. Em produção use autenticação e TLS.

listener 1883
protocol mqtt

allow_anonymous true

persistence true
persistence_location /mosquitto/data/

log_dest stdout
log_type error
log_type warning
log_type notice
log_type information
